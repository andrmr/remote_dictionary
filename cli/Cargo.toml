[package]
name = "cli"
version = "0.1.0"
edition = "2021"

[dependencies]
client = { path = "../client" }
common = { path = "../common" }

anyhow = "1.0.66"

clap = { version = "^4.0.18", features = ["derive"] }

tokio = { version = "^1.21", features = ["full"] }
